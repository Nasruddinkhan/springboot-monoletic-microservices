server.port =${PORT:0}
eureka.instance.instance-id=${spring.application.name}:${spring.application.instance-id:${random.value}}
spring.application.name = integration-app-ws
eureka.client.service-url.default-zone=http://localhost:8761

